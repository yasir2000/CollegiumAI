import React from 'react';
import { Box, Typography } from '@mui/material';

const FacultyPage: React.FC = () => (
  <Box>
    <Typography variant="h4" fontWeight="bold" gutterBottom>Faculty</Typography>
    <Typography variant="subtitle1" color="text.secondary">Faculty management and resources</Typography>
  </Box>
);

export default FacultyPage;