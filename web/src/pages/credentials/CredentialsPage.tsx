import React from 'react';
import { Box, Typography } from '@mui/material';

const CredentialsPage: React.FC = () => (
  <Box>
    <Typography variant="h4" fontWeight="bold" gutterBottom>Credentials</Typography>
    <Typography variant="subtitle1" color="text.secondary">Digital credential management and verification</Typography>
  </Box>
);

export default CredentialsPage;